% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install_binaries.R
\name{install_binaries}
\alias{install_binaries}
\title{Install AquaCrop Binary}
\usage{
install_binaries(
  version = NULL,
  os = NULL,
  path = getwd(),
  force = FALSE,
  compiler = NULL,
  keep_source = FALSE
)
}
\arguments{
\item{version}{Character. AquaCrop version to install. If \code{NULL}, installs
the latest supported binary release. Accepted formats include \code{"7.1"},
\code{"v7.1"}, and \code{"7.1.0"}. Use \code{"dev"} to compile from source.}

\item{os}{Character. Operating system: \code{"windows"}, \code{"linux"}, or \code{"macos"}.
If \code{NULL}, detected automatically.}

\item{path}{Character. Installation directory. Defaults to the current
working directory.}

\item{force}{Logical. If \code{TRUE}, reinstalls an existing executable.
Default: \code{FALSE}.}

\item{compiler}{Character or \code{NULL}. Fortran compiler used only when
\code{version = "dev"}. If \code{NULL}, detected automatically.}

\item{keep_source}{Logical. Keep source code after compilation when
\code{version = "dev"}. Default: \code{FALSE}.}
}
\value{
Invisibly returns the installed AquaCrop version.
}
\description{
Downloads and installs the AquaCrop executable for the requested operating
system. The function automatically detects the current platform, resolves
the requested AquaCrop version, caches downloaded archives, and installs
the executable in the selected directory.
}
\details{
Use \code{version = "dev"} to compile the latest development version from source.
}
\examples{
\dontrun{
install_binaries()
install_binaries(version = "7.1", path = "~/aquacrop")
install_binaries(version = "dev", path = "~/aquacrop")
install_binaries(force = TRUE)
}

}
