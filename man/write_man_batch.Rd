% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_man_batch.R
\name{write_man_batch}
\alias{write_man_batch}
\title{Write AquaCrop MAN files in batch}
\usage{
write_man_batch(
  site_name = NULL,
  params = NULL,
  path = "MANAGEMENT/",
  eol = NULL,
  climate_path = "CLIMATE/",
  base_path = getwd(),
  verbose = TRUE,
  clean = FALSE
)
}
\arguments{
\item{site_name}{Character vector or \code{NULL}. Names of stations to process:
\itemize{
\item If \code{NULL}, all stations are automatically discovered from \code{.CLI} files in the \verb{CLIMATE/} directory.
\item If a vector, only the specified stations will be processed; all must have corresponding climate files.
}}

\item{params}{Either:
\itemize{
\item A single named \code{list} of management parameters (var_02 to var_21), applied to all stations, or
\item A \code{list} of named \code{list}s (one per station), each containing management parameters.
The length of the list must match the number of stations.
Set to \code{NULL} or \code{list()} to use all default values for all stations.
See \code{\link{ManData}} documentation for further details.
}}

\item{path}{Output directory path. Default: \code{"MANAGEMENT/"}.}

\item{eol}{End-of-line character style. Options: "windows","linux", or "macos". If \code{NULL} (default), eol is auto-detected.}

\item{climate_path}{Path to climate file directory for auto-discovery. Default: \code{"CLIMATE/"}.}

\item{base_path}{Base absolute path. Default: current working directory.}

\item{verbose}{Logical. If \code{TRUE} (default), prints progress messages. If \code{FALSE}, runs silently.}

\item{clean}{Logical. If \code{TRUE}, removes existing \code{.MAN} files
from \code{path} before writing new files. Default: \code{FALSE}.}
}
\value{
Invisibly returns \code{NULL}. The main effect is writing MAN files to the specified directory.
}
\description{
Generate AquaCrop MAN files for multiple stations in a batch.
}
\details{
The function validates that all specified stations have corresponding climate files.

A single \code{list} of parameters will be automatically applied to all stations.

Valid parameters include var_02 through var_21. See \code{\link{ManData}}
documentation for further details on parameter descriptions and valid ranges.

Use this function when you need to create MAN files for \strong{multiple stations at once}.
}
\examples{
\dontrun{
# Example 1: Multiple stations, same management parameters
base_params <- list(var_03 = 20, var_04 = 60, var_05 = 50)
stations <- c("grid_001", "grid_002")
write_man_batch(
  site_name = stations,
  params = base_params
)

# Example 2: Multiple stations, different management parameters
params_list <- list(
  list(var_03 = 0, var_04 = 0, var_05 = 30), # Low fertility, no mulch
  list(var_03 = 60, var_04 = 80, var_05 = 70) # High fertility, heavy mulch
)
write_man_batch(
  site_name = stations,
  params = params_list,
  path = "MANAGEMENT/"
)

# Example 3: Auto-discover all stations, default parameters
write_man_batch(
  site_name = NULL,
  params = NULL,
  base_path = "/my/project/path",
  verbose = FALSE
)

# Example 4: Station-specific fertility gradient
params_list <- list(
  list(var_05 = 30), # Station 1: low fertility
  list(var_05 = 60), # Station 2: medium fertility
  list(var_05 = 90) # Station 3: high fertility
)
write_man_batch(
  site_name = c("site_A", "site_B", "site_C"),
  params = params_list
)
}

}
\seealso{
\code{\link{write_man}} for single management file generation,
\code{\link{write_prm_batch}} for batch PRM file generation,
\code{\link{ManData}} for complete parameter reference

Other batch operations: 
\code{\link{write_gwt_batch}()},
\code{\link{write_irr_batch}()},
\code{\link{write_obs_batch}()},
\code{\link{write_off_batch}()},
\code{\link{write_prm_batch}()},
\code{\link{write_sol_batch}()}
}
\concept{batch operations}
