% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_sol_batch.R
\name{write_sol_batch}
\alias{write_sol_batch}
\title{Write AquaCrop SOL files in batch}
\usage{
write_sol_batch(
  site_name = NULL,
  params = NULL,
  path = "SOIL/",
  eol = NULL,
  verbose = TRUE,
  clean = FALSE,
  write_sw0 = TRUE,
  initial_water = "WP",
  salinity = "none",
  initial_cc = -9,
  initial_biomass = 0,
  initial_root_depth = -9,
  bund_water = 0,
  bund_ec = 0,
  swo_layers = NULL,
  climate_path = "CLIMATE/",
  base_path = getwd()
)
}
\arguments{
\item{site_name}{Character vector or \code{NULL}. Names of sites to process:
\itemize{
\item If \code{NULL}, all sites are automatically discovered from \code{.CLI} files in the \verb{CLIMATE/} directory.
\item If a vector, only the specified sites will be processed.
}}

\item{params}{Either:
\itemize{
\item A single named \code{list} of soil parameters (cn, rew, texture, thickness), applied to all sites, or
\item A \code{list} of named \code{list}s (one per site), each containing soil parameters.
The length of the list must match the number of sites.
Set to \code{NULL} or \code{list()} to use all default values for all sites.
Valid parameters: \code{cn}, \code{rew}, \code{texture}, \code{thickness}.
}}

\item{path}{Output directory path. Default: \code{"SOIL/"}.}

\item{eol}{End-of-line character style. Options: "windows","linux", or "macos". If \code{NULL} (default), eol is auto-detected.}

\item{verbose}{Logical. If \code{TRUE} (default), prints progress messages. If \code{FALSE}, runs silently.}

\item{clean}{Logical. If \code{TRUE}, removes existing \code{.SOL} and \code{.SW0} files
from \code{path} before writing new files. Default: \code{FALSE}}

\item{write_sw0}{Logical; if \code{TRUE}, also write SW0 files. Default: \code{TRUE}.}

\item{initial_water}{Initial water content specification for SW0 files. Default: \code{"WP"}.}

\item{salinity}{Salinity specification for SW0 files. Default: \code{"none"}.}

\item{initial_cc}{Initial canopy cover for SW0 files. Default: \code{-9.00}.}

\item{initial_biomass}{Initial biomass for SW0 files. Default: \code{0.000}.}

\item{initial_root_depth}{Initial root depth for SW0 files. Default: \code{-9.00}.}

\item{bund_water}{Bund water for SW0 files. Default: \code{0.0}.}

\item{bund_ec}{Bund EC for SW0 files. Default: \code{0.00}.}

\item{swo_layers}{Optional layers for SW0 files. Default: \code{NULL}.}

\item{climate_path}{Path to climate file directory for auto-discovery. Default: \code{"CLIMATE/"}.}

\item{base_path}{Base absolute path. Default: current working directory.}
}
\value{
Invisibly returns \code{NULL}. The main effect is writing SOL (and optionally SW0) files
to the specified directory.
}
\description{
Generate AquaCrop SOL (and optionally SW0) files for multiple sites in a batch.
}
\details{
When \code{site_name = NULL}, the function discovers sites from \code{.CLI} files in the climate directory.

A single \code{list} of parameters will be automatically applied to all sites.

Valid parameters in \code{params} include \code{cn}, \code{rew}, \code{texture}, \code{thickness}.
SW0-related parameters (\code{initial_water}, \code{salinity}, etc.) are passed as direct arguments
to \code{write_sol_batch()} and will be applied to all sites.

See \code{\link{write_sol}} documentation for parameter descriptions and valid ranges.

Use this function when you need to create SOL files for \strong{multiple sites at once}.
}
\examples{
\dontrun{
# Example 1: Multiple sites, same soil profile
soil_params <- list(
  texture = c("sandy loam", "clay loam"),
  thickness = c(0.4, 0.6),
  cn = 65,
  rew = 7
)
sites <- c("grid_001", "grid_002", "grid_003")
write_sol_batch(
  site_name = sites,
  params = soil_params
)

# Example 2: Multiple sites, different soil profiles
params_list <- list(
  list(texture = "sandy loam", thickness = 1.0, cn = 55, rew = 5),
  list(texture = "loam", thickness = 1.0, cn = 70, rew = 6),
  list(texture = c("clay", "clay loam"), thickness = c(0.3, 0.7), cn = 80, rew = 7)
)
write_sol_batch(
  site_name = sites,
  params = params_list,
  path = "SOIL/",
  initial_water = "FC",
  salinity = "none"
)

# Example 3: Auto-discover all sites, default soil parameters
write_sol_batch(
  site_name = NULL,
  params = NULL,
  base_path = "/my/project/path",
  verbose = TRUE
)
}

}
\seealso{
\code{\link{write_sol}} for single soil file generation,
\code{\link{write_swo}} for initial water content files,
\code{\link{write_man_batch}} for batch management file generation,
\code{\link{SoilWater}} for hydraulic property reference

Other batch operations: 
\code{\link{write_gwt_batch}()},
\code{\link{write_irr_batch}()},
\code{\link{write_man_batch}()},
\code{\link{write_obs_batch}()},
\code{\link{write_off_batch}()},
\code{\link{write_prm_batch}()}
}
\concept{batch operations}
