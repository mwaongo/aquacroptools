% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_outputs.R
\name{read_day_out}
\alias{read_day_out}
\title{Read an AquaCrop day.out file}
\usage{
read_day_out(file, na = .DAY_NA_VALS, verbose = FALSE)
}
\arguments{
\item{file}{Path to the \code{day.out} file.}

\item{na}{Numeric sentinel values coerced to \code{NA}.
Default: \code{c(-9, -9.9, -99, -99.9, -999, -999.9)}.}

\item{verbose}{If \code{TRUE}, prints column count and duplicate names.}
}
\value{
A \link[tibble:tibble]{tibble} with a trailing \code{prm_file} column
(base name of the \code{.PRM} file inferred from \code{file}).
}
\description{
Auto-detects columns and multi-run structure. Handles duplicated column
names (suffixed \verb{_1}, \verb{_2}, ...) and space-separated numeric suffixes
(e.g. \code{"WC 2"} becomes \code{WC2}).
}
\examples{
\dontrun{
# Single-run file
df <- read_day_out("path/to/day.out")
df

# Multi-run file with diagnostic info
df <- read_day_out("path/to/day.out", verbose = TRUE)

# Filter one year after reading
library(dplyr)
df |> filter(Year == 1976)
}

}
